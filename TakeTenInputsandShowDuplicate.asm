INCLUDE "EMU8086.INC"
.MODEL SMALL
.STACK 100H

.DATA

	A1 DB 10 DUP(?)

.CODE

MAIN PROC
	
	MOV AX, @DATA
	MOV DS, AX
	
	MOV CX, 10
	MOV BX, 0
	MOV AH, 1
	
	WHILE:
	
		INT 21H
		MOV A1[BX], AL
		INC BX
	
		LOOP WHILE
	
	
	MOV CX, 10
	MOV BX, 0
	MOV AH, 2
	
	PRINTN
	
	WHILE2:
	
		MOV DL, A1[BX]
		;ADD DL, 48
		INT 21H
		INC BX
	
		LOOP WHILE2
	
	
	MOV AH, 4CH
	INT 21H
	MAIN ENDP

END MAIN
